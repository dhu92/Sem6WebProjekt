<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Recipes</title>
    <style>{{ source('css/main.css') }}</style>
</head>
<body>

<ul>
    {% for recipe in recipes%}
        <li>{{ recipe.name }}</li>
    {% else %}
        <li>No recipes found</li>
    {% endfor %}
</ul>

</body>
</html>