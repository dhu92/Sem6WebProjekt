{% extends 'jquery.collection.html.twig' %}

{% block extra_js %}
    <script src="{{ asset('js/jquery.js') }}"></script>
    <script src="{{ asset('js/jquery.collection.js') }}"></script>
{% endblock %}

{% block title %}Add Recipe{% endblock %}

{% block body %}
<div>
    {{ form_start(recipe_form) }}
    {{ form_widget(recipe_form) }}

    {%
        form_theme setIngredient
        'jquery.collection.html.twig'
        'buttonTemplate.html.twig'
    %}
    {{ form(setIngredient) }}

 <!--   <div class="form-group row">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <input type="submit" value="Send" class="btn btn-success">
        </div>
    </div>
-->

    {% for ingredient in setIngredientData.ingredients %}
<!--        <div class="col-xs-4">
            {{ form_errors(ingredient.name) }}
            {{ form_row(ingredient.language) }}

            --><p>Value : {{ ingredient.name }}</p><!--

        </div>-->
    {% endfor %}
    {{ form_end(recipe_form) }}
</div>

{% endblock %}


{% block script %}

    <script type="text/javascript">
        $('.setIngredient-collection').collection();//({allow_duplicate: false});
    </script>

{% endblock %}